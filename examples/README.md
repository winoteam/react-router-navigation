# Run the example
Clone the repo:
```shell
$ git clone https://github.com/LeoLeBras/react-native-router-navigation.git
$ cd examples/Basic
```

Install Node.js components:
```shell
$ npm i
```

Run the example:
```shell
$ npm start
```
