/* @flow */

import React from 'react'
import { View, Text } from 'react-native'
import styles from './styles'

const SearchScene = (props): React$Element<any> => {
  return (
    <View style={styles.container}>
      <Text>SearchScene</Text>
    </View>
  )
}

export default SearchScene
